name: paper_checker
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.20.0 # 2022/07/24 installed

  flutter_riverpod: ^2.0.0-dev.6 # 2022/07/24 installed
  freezed_annotation: ^2.1.0 # 2022/07/24 installed
  json_serializable: ^6.3.1 # 2022/07/24 installed

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: ^2.2.0 # 2022/07/24 installed
  freezed: ^2.0.4 # 2022/07/24 installed

flutter:

  uses-material-design: true
